let me explain you about the clear architecture again.
1. There are members. This is the main account users login. And these are root elements in our revelo arichitecture structure.
2. Each members can create multiple accounts. (member : account = 1 : n)
3. Each accounts can have multiple jobs. (account : job = 1 : m)
4. Each job can have multiple tasks group. In terms of tasks group, it contains multiple tasks. Each tasks group has status of submitted, approved, rejected.

Check the project and findout if these logics implemented correctly.
And if something went wrong with this, Fix them