Each user has their own prompts so each user should be able to CRUD prompts like git issues.
Prompt posters can share their prompts with others. Each user can have many prompts and can set one as their main prompt.
When a user has 1 prompt, that prompt becomes that user's main prompt.
the prompt sample data is data.txt
You have to create prompt table in db.
For prompt edits, you have to use plain text editor like notepad.
For prompt views, you have to use nice text viewer with modal

Now I am gonna create api endpoints.
All endpoints that I create should be POST type.
You have to make this endpoints:
1. when an api request (/v1/issue) to get a github issue, you have to verify their user id and search git issues (user's issue or shared issue with taken flag = open) in db. You have to pick the old issues first. You also should fetches the prompt from db. You have to return issue + prompt as response. You have to set the taken status as "in progress"

2. when an api request to set a github issue done, you have to verify user id and issue id (if you don't have in db, create issue_id field) and set the taken status true.

I wanna make client side program using python.
I wanna build this program on Ubuntu.
I wanna make executable .deb or similar file and wanna install using sudo apt install command.
Make a client side program with GUI and make it executable file and make it possible to install and install it on my ubuntu system.

This is what client side program need to have.
1. Make sign in feature to the website (You can refer website's signin approach)
2. when you click "get New Issue button", you have to send api (https://www.talentcodehub.com/v1/issue) and if you get response, you display it on the program in detail.

In terms of client side program, you can create it in /deb directory (create new)